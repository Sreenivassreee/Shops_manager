import 'package:flutter/material.dart';
class MainSaleDetailsScreen extends StatelessWidget {
  const MainSaleDetailsScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Container(
          color: Colors.red,
          child: Text("HomePage"),
        ),
      ),
    );
  }
}
